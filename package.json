{
  "name": "ast-search-workspace",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "tree-sitter",
      "tree-sitter-python"
    ]
  },
  "scripts": {
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "multi-semantic-release": "^3.1.0",
    "semantic-release": "^25.0.3"
  }
}
